# Project Guide

This project has been scaffolded to match the requested folder structure.

## Next steps
- Replace placeholder route pages under `app/[locale]`.
- Add localized message files under `messages`.
- Add static assets under `public`.
